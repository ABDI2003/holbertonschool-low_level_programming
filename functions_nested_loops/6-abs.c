#include "main.h"
#include <stdlib.h>

/**
 * _abs - computes the absolute value of an integer using abs function
 * @n: the integer to find the absolute value of
 * Return: The absolute value of n
 */

int _abs(int n)
{
	return (abs(n));
}
